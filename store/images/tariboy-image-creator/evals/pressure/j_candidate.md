Read catalog files and these skill bodies: `using-superpowers`, `tariboy-image-delivery`, `tasks`, `tariboy-image-authoring`, `writing-skills`, `test-driven-development`, and `verification-before-completion`.

Proposed next actions for IMG-11, without executing:

1. Reuse the approved plan; inspect `/tmp/store` Git state and read `/tmp/store/skills/review/SKILL.md` plus its relevant consumers.
2. Apply `writing-skills` to this independent Store skill: establish failing baseline scenarios before edits, record observed failures, make the smallest targeted change, rerun the same scenarios, and refine until verified. Use wording micro-tests where applicable.
3. Preserve isolation: reuse/create the task worktree for Git; for non-Git, the recorded approval permits editing in place.
4. Report skill-specific evidence and changed files through IMG-11 using the Store’s applicable delivery path. Whole-image evaluation and image version/build changes are not triggered by this scope.

No services, writes, Native Task calls, or completion calls were executed.
