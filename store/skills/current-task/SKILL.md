---
name: current-task
description: Use when an agent needs to attribute Tariboy AI usage to a Native Tasks item.
---

# Current task

Use `scripts/current_task.sh KEY` when work begins or changes. Use `scripts/current_task.sh --clear` when no task should receive further attribution.
