---
name: loop
description: Use when an agent needs to complete its Tariboy iteration or control its own iteration loop.
---

# Iteration loop

Use `scripts/loop.sh done` to finish the iteration (`i-am-done`), optionally with `--idle`. Use `scripts/loop.sh start` or `scripts/loop.sh stop` to control the loop.
