## Schedule
The Python script lives inside this skill directory under `scripts/`.
You can schedule future wake-ups or channel publications. Load the packaged
`schedule` skill for the commands and lifecycle.
